spring:
  application:
    name: customer_service
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
#    stream:
#      bindings:
#        output:
#          destination:  customerChangeTopic
#          content-type: application/json
#      kafka:
#        binder:
#          zkNodes: localhost
#          brokers: localhost
